# (superseded)

This file is replaced by the test kit, which uses an improved build of the DLL
that reads a shared "match code" from `patch_ranked.txt` (so you can match a
specific person privately).

- To test with someone: send them **`patch_ranked_TESTKIT.zip`** and follow
  **`patch_ranked_TESTKIT/START_HERE.md`**.
- Your own machine has already been updated to the new build and set to match
  code **314159** (`patch_ranked64.dll` + `patch_ranked.txt` in this folder).
