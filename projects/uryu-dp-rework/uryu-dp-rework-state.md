---
name: uryu-dp-rework-state
description: "State/findings of Berg's Uryu (pl003) sp_atk02 counter-to-DP rework balancing"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3af549cc-8a4b-4d46-9fa0-c38e714e2ede
---

Berg is reworking Uryu's sp_atk02 counter into a DP in Bleach: Rebirth of Souls. Real rework files received 2026-07-03 (first upload was pure vanilla). tcmbpkg rework: sp_atk02_1 nodes RecvEvent/hitDamage -> AutoCombo/"" and sp_atk02 nodes drop hitDamage (x3 tiers; sp_atk01_1 counter still uses hitDamage -- never touch it with global replaces). tadjpkg rework highlights: sp_atk02_1 melee now anim 5-20 (was 4-8; Protect invuln still 0-15, so active 15-20 is unprotected), bigger radii, arrow homing, rev damage 1400, ObjectSpeedChange counter freeze 0.001->0.024.

Speed knowledge: WorldSpeedChange (param "Speed", world multiplier, ANIM-frame window, real duration = span/Speed, 60fps confirmed) is NOT what true DPs use -- Ichigo's DP slows the OPPONENT via ObjectSpeedChange while he runs full speed. Ichigo values now cloned onto Uryu (byte-identical): stance = window 0->60 frame=-1 speed=0.01 end_change_frame=0 end_change_speed=1.0; _1 = window 0->60 frame=-1 speed=0.10 end_change_frame=10 end_change_speed=0.2 (uids 0x51D0AA61-63/71-73). Layered design (Berg 2026-07-03/04): Ichigo ObjectSpeedChange clone + WSC timeslow curve on _1 (5,0.001)(5,0.01)(5,0.1) + heavy-attack intro animation. Intro implementation (patch_dp_intro_anim.py): tactpkg (PZZE zlib @0x18, u32 usize @8; inner actmng_pkg with tadjpkg-style 72B entry table; entries = act_data JSON mapping component slots to tmo motion clips; 4 entries share name "pl003" incl. one 38.9MB motion-data blob -- NEVER rebuild by name, use index) stance act_data retargeted to each tier's atk_hi01 motion set; tcmb sp_atk02_1 nodes variables[12] (AutoCombo min valid frame, per header memo) = 12. FINAL version (2026-07-04): intro 20 frames (tcmb variables[12]=20), _1 invuln 0->80, melee/cancel left to Berg's hand edits (script sect 4 = None/off). RECOVERY (in-game tested facts): SlowMotionRate = NO-OP for this, retired; CancelTiming does NOT gate the natural end-of-action return to neutral (only cancels during the action) -- actionable moment = motion end -> act_data next_name. Working mechanism v2 (unconditional act_data exit played the stagger even on HIT -> replaced): act_data exit stays ntrl_in; patcher WHIFF_STAGGER adds per-tier tcmb nodes dam_fail/evo_/rev_ (uids 900-902, AutoCombo, vars[12/13]=100/200, vars[14] hit_combo_stop=-1 = derive only when NO normal hit) appended to sp_atk02_1's nexts. Hit -> normal recovery; whiff -> stagger. IN-GAME FINDING: act_data next chaining (dam_fail -> dam_parry) is a NO-OP when the action was entered via a combo-graph node (graph precedence on action end) -- retired. Current doubling attempt: dam_fail act_data play_frame 1.0 -> 0.5 (patcher STAGGER_PLAY; hypothesis play_frame = frame-advance rate, cf. cmp_play_speed) = 90 anim frames over 180 real. UNVERIFIED in-game. Fallback if no-op: retarget dam_fail tmo/window to a longer motion section, e.g. pl003_dam_parry_blown 180->370 get-up (window mechanics proven). Berg tuned whiff branch window to 110-300 in patcher himself. Hit-only install (script v9 sect 6): Enhance record (install granter, enhance_start=1 max_val=1800~30s; enhance_active on ATTACK records is a state CONDITION for the paired -1/1 arrow variants, not a granter) retimed 6->10 to 112->120 on base+evo _1 (rev has none, vanilla design) -- frames past the whiff-branch exit (~110) are only reached on connect. COUPLING: ENHANCE_HIT_WINDOW must stay just above the patcher's WHIFF_STAGGER min_frame or install goes unconditional again. Untested: whether guarded hit counts as "hit" for vars[14] (if blocked DP doesn't stagger, try vars[15]). Note: vanilla tcmb reuses uniqueIDs 401-418 across two tables (Bandai's own duplication, harmless). Engine segments motions via act_data start/end_frame (in/loop/out patterns, e.g. dam_parry_blown chain 0-120-180-370); next_name chaining across actions is standard; ct_* group = cutscene actions (ct_start=round intro, not counter). Berg's hand-tuned frame data: base melee 5-20 cancel 110 (possibly un-retuned: cancel 110 anim = ~real 140 under tail slow, only +30 lock), evo 20-35/180, rev 17-37/300. Outputs: pl003_final.tactpkg/.tcmbpkg/.tadjpkg. Stance tadj SEs still counter-flavored under heavy visuals (accepted). True-DP reference pl051 Ichigo (files in project): his sp_atk02 = ONE action, Protect 0->70 type=完全無敵 protect_react=ヒット判定あり from frame 0 through the hit (melee 68-69); Uryu was grabbable at activation because the sp_atk02 stance remnant had NO Protect and _1 used counter-style 判定なし. Protect type vocabulary seen in pl051: 完全無敵, 弾無敵, 崩しに無敵 (grab/kuzushi), リバースに無敵, 通常攻撃とSP技に無敵 -- categories stack as separate records with independent windows. add_speed_sp_atk02_1.py v4 (project root) = 3 idempotent sections: ObjectSpeedChange clone, CURVE (default empty), Protect (stances 0->120 uid 0x51D0AA41-43, _1 0->15 uid 0x51D0AA51-53). Output: Reworked Uryu/pl003_dp2.tadjpkg (dp/modded/modded_v2/curve names tombstoned in session mounts after Berg moves files out -- always write a fresh name). Melee active 5-20 vs invuln 0-15 gap left intentionally (DAMAGE_INVULN_END setting raises it).

Berg's tcmb editor corrupts files: it mojibake'd all Japanese memos and clamped the file to its original byte size, truncating the tail (lost rev-table atk_lo03_d01 node + closing braces). Repaired as Reworked Uryu/pl003_repaired.tcmbpkg = vanilla + exactly the 9 functional lines (verified). Future tcmb edits should start from the repaired file, not the clamped one. Vanilla tcmb has a pre-existing malformed shift-jis byte @1150 (game tolerates). See [[tadjpkg-format]].
